interface UserProfileInfoType {
  imgUrl: string;
  fullname: string;
  username: string;
  createdAt: string;
}

const UserProfileInfo = ({
  imgUrl,
  fullname,
  username,
  createdAt,
}: UserProfileInfoType) => {
  return <div></div>;
};

export default UserProfileInfo;
